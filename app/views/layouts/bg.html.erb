<!DOCTYPE html>
<html>
  <head>
    <title><%= t("name")%></title>
    <%= render :partial => "partials/head" %>
  </head>
  <body>
    <%= render :partial => "partials/navigate"%>
    <div id="wrap">
      <div id="content">
        <%= render :partial => "partials/header_tab", 
          :locals => {:tab_name => "settings"} %>

        <div class="body-left", style="width: 820px;">
          <%= yield :body_left %>
        </div>
        <div class="body-right" style="width: 160px;">
          <%= yield :body_right  %>
        </div>
      </div>
      <%= render :partial => "partials/foot" %>
    </div>
  </body>
</html>
